# Web framework
fastapi==0.126.0
uvicorn[standard]==0.38.0
# 0.0.31 закрывает 6 CVE парсера multipart (pip-audit 2026-07-04, ADR-074) —
# это поверхность загрузки артефактов/форм.
python-multipart==0.0.31

# Database
SQLAlchemy==2.0.45

# Docker orchestration
docker==7.1.0

# Auth / security
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1
cryptography==49.0.0   # SecretBox (app/cloud/secret_box.py, Б3)

# Reverse-proxy client (proxy.py)
httpx==0.28.1
